name: Hello World!

on:
  push:
    branches:
      - main
      # note that your "main" branch might be called main instead of master

jobs:
  hello_job:
    steps:
      - name: Say hello world
        run: |
          echo "Hello World!"